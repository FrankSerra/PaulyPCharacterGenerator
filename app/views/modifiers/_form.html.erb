<%= form_with(model: modifier, local: true) do |form| %>
  <% if modifier.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(modifier.errors.count, "error") %> prohibited this modifier from being saved:</h2>

      <ul>
      <% modifier.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :replacewithshape %>
    <%= form.check_box :replacewithshape %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
