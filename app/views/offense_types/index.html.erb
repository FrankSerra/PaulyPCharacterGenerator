<p id="notice"><%= notice %></p>

<h1><%= OffenseType.label %>s</h1>
<h4>Weapons must be added to a <%= OffenseType.label %> to be chosen. Click "show" to see them.</h4>
<h4><%= OffenseType.label.pluralize %> must be added to a Resource to be chosen by that Resource. Click "show" to see them.</h4>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th># Weapons</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @offense_types.each do |offense_type| %>
      <tr>
        <td><%= offense_type.name %></td>
        <td><%= offense_type.numberofweapons %></td>
        <td><%= link_to 'Show', offense_type %></td>
        <td><%= link_to 'Edit', edit_offense_type_path(offense_type) %></td>
        <td><%= link_to 'Destroy', offense_type, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Offense Type', new_offense_type_path %>
